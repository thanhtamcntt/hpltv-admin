import React from 'react';

import { Layout } from 'antd';

const { Footer } = Layout;

function FooterAdmin() {
  return (
    <Footer
      style={{
        textAlign: 'center',
        padding: '0',
      }}
    />
  );
}
export default FooterAdmin;
